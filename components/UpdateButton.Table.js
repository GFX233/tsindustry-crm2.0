const UpdateButton = ({onClick}) => {
    return (
        <button onClick={onClick} className="btn btn-circle btn-outline btn-info btn-xs">
            ...
        </button>
    )
}

export default UpdateButton